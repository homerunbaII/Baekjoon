SELECT F.FOOD_TYPE, F.REST_ID, F.REST_NAME, F.FAVORITES
FROM REST_INFO AS F
INNER JOIN (SELECT FOOD_TYPE, MAX(FAVORITES) AS FAVORITES
FROM REST_INFO
GROUP BY FOOD_TYPE) AS S
ON F.FOOD_TYPE = S.FOOD_TYPE AND F.FAVORITES = S.FAVORITES
ORDER BY F.FOOD_TYPE DESC